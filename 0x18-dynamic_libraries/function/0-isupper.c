#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

/**
 * _isupper - checks for uppercase character.
 * @c: Returns 1 if c is uppercase
 * @c: Returns 0 otherwise
 * Return: c
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
