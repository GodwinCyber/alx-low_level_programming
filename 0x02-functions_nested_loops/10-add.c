#include "main.h"

/**
 * add -  adds two integers and returns the result
 * @a: stdout
 * @b: stdout
 * Return: Always (Success)
 *
 */

int add(int a, int b)
{
	int result = a + b;

	return (result);
}
